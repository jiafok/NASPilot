# NASPilot Product Strategy

状态约束:
- 当前开发完成度约 70%
- 不新增功能
- 不新增页面
- 不新增插件
- 本文仅基于现有代码审计

## 1. 产品定位

NASPilot 的正确定位不是 通用 DevOps 平台，而是:

面向单机或小规模 HomeLab/NAS 的 自动化运维控制台。

核心价值:
- 把零散脚本执行、定时任务、插件任务、日志排障、容器运维集中到一个入口
- 降低 SSH + crontab + 多脚本手工协作成本
- 把“能跑”提升为“可观测、可回溯、可重复执行”

边界定义:
- 做 NAS 运维自动化编排与可视化
- 不做多租户企业级平台
- 不做完整 CI/CD 平台
- 不做广义资产管理系统

## 1.1 四大产品主轴

NASPilot 的产品结构应固定为四个主轴，不再扩展并行主线:

- Task: 通用任务编排与执行中心
- Plugin: 业务自动化能力中心
- Container: 容器运维与应急操作中心
- Log: 观测、排障与审计中心

这四个主轴构成完整运维闭环:
- Task 负责计划与执行
- Plugin 负责场景化自动化
- Container 负责运行时控制
- Log 负责结果验证与故障定位

## 2. 核心用户

### 核心用户画像 A: 家庭/个人 NAS 管理员
- 典型特征: 维护 1-3 台 NAS，依赖 Docker、下载、网盘、备份脚本
- 目标: 每天 5-20 分钟完成巡检，故障时 1 个界面定位问题

### 核心用户画像 B: 轻量 HomeLab 运维者
- 典型特征: 使用容器服务组合，具有基本脚本能力
- 目标: 通过计划任务与插件减少重复劳动，降低人工操作风险

### 非核心用户
- 需要复杂 RBAC、审批流、组织协作的大型团队
- 需要跨集群编排与高可用控制面的企业用户

## 3. 核心场景

按频次和业务价值排序:

1. Task 场景: 周期任务编排与失败重跑
- 在任务中心维护命令、参数、调度与超时
- 失败后可直接进入日志定位

2. Plugin 场景: 场景自动化配置与执行
- 管理 PT RSS、AList、Cloudflare、备份等插件实例
- 通过运行历史判断策略是否生效

3. Container 场景: 容器级巡检和应急
- 查看运行状态与资源占用
- 执行批量操作、容器日志、容器内命令

4. Log 场景: 统一观测与故障闭环
- 结构化日志筛选
- 原始日志追踪
- 回溯任务和插件执行结果

## 4. 产品阶段判断

当前 70% 完成度的本质:
- 功能面较完整
- 结构一致性不足
- 信息架构仍偏“模块堆叠”，尚未完全转为“用户任务流”

冻结期正确目标:
- 做收敛，不做扩张
- 做整合，不做新入口
- 做稳定性和可理解性，不做新概念

## 5. 产品策略结论

### 战略主线

把 NASPilot 从 功能集合 变成 运维工作台。

### 三个战略抓手

1. 入口收敛
- 减少同一能力的多入口导航
- 统一用户心智: 从巡检到执行到排障

2. 信息优先级重排
- 首页展示风险和行动，不展示次要信息堆叠
- 让用户先看到 要处理什么，而不是 系统有什么

3. 页面关系清理
- 明确主从页与详情页
- 清理重复页、重复路由、重复命名

## 6. Dashboard 应展示什么

Dashboard 定义为 今日运维总览 + 立即行动入口。

应严格按 Task / Plugin / Container / Log 四域展示。

应包含 4 个区域:

1. Container 总览
- CPU、内存、磁盘、在线时长
- 容器运行中数量、异常容器数量

2. Task 总览
- 最近 24h 执行总数
- 成功率
- 失败任务数与最近失败任务

3. Plugin 总览
- 关键插件最近一次状态
- 待配置插件数量
- 最近失败插件 Top N

4. Log 与行动区
- 最近错误日志摘要
- 一键进入失败任务日志
- 一键进入失败插件日志
- 跳转对应修复页面

不建议继续强化的内容:
- 仅展示原始统计但无行动指引
- 与详细页重复的重表格展示

## 7. 冻结期验收标准

如果不增加任何功能，仍可达成的产品验收:

- 用户首次登录后 30 秒内知道去哪里执行日常巡检
- 任务失败后 3 次点击内到达日志与修复入口
- 关键插件状态在首页可判断 成功/失败/未配置
- 页面命名和导航分组可被非开发用户理解

## 8. 一句话战略声明

NASPilot 在本阶段应成为 面向 NAS 单管理员的高频运维工作台，以稳定性、可观测性和入口一致性作为冻结期核心目标。
